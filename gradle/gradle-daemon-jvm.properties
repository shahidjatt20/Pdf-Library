# Daemon JVM criteria: require Java 17, no specific vendor.
# The original file pinned toolchainVendor=JETBRAINS (JBR) 21, which JitPack cannot
# provision (the JBR archive unpacks without a valid Java home). A plain version-only
# requirement is satisfied by any installed/temurin JDK 17.
toolchainVersion=17
